import React from 'react';

const UserProfile = () => {
    console.log('UserProfile rendered');
    return (
        <div>
            <h1>User Profile</h1>
        </div>
    );
};

export default UserProfile;
